{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --workers 1 --threads 1 --timeout 0 --bind 0.0.0.0:$PORT wsgi:app",
    "restartPolicyMaxRetries": 5,
    "restartPolicyWindowMs": 60000
  }
}
